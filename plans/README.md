# Implementation Plans

Generated by the `improve` skill on 2026-06-14 (audit at commit `3398a0d`).
Execute in the order below unless dependencies say otherwise. Each executor: read
the plan fully before starting, honor its STOP conditions, and update your row
when done.

**This repo uses a binding `CLAUDE.md` ("DOX") hierarchy.** Every plan's executor
must read the root `CLAUDE.md` plus the chain for any path they touch ("Read
Before Editing"), and update the nearest owning `CLAUDE.md` when a change alters
a contract — the affected plans call out exactly which docs to update.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Restore a green build (remove dead `_index`) | P1 | S | — | DONE — branch `advisor/001-fix-broken-build` |
| 002  | Correct README monster count 48 → 72 | P3 | S | — | DONE — branch `advisor/002-readme-monster-count` |
| 003  | `bun test` baseline + game/catalog characterization & frozen-seed guard | P1 | M | 001 | DONE — branch `advisor/003-test-baseline` (57 tests) |
| 004  | Move debug round-sim helpers to `src/game/debug.ts` | P2 | M | 001, 003 | DONE — branch `advisor/004-extract-debug-logic` (64 tests) |
| 005  | Extract shared `useGateReveal` hook | P3 | S | 001 | DONE — branch `advisor/005-gate-reveal-hook` |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

**Recommended sequence**: **001 first and immediately** — until it lands, `main`
does not build and GitHub Pages is not deploying, and every other plan's done
criteria (which run `typecheck`/`build`) will fail. 002 is independent and
trivial (bundle it with 001). Then 003 (establishes the test harness), then 004
(reuses that harness and adds tests for the moved code), with 005 anytime after
001.

## Dependency notes

- **001 unblocks everything.** `tsc -b --noEmit` exits 1 on `RoundScreen.tsx:21`
  today; deploys (`tsc -b && vite build`) have been failing since `0ff78fe`. Any
  plan whose done criteria run typecheck/build is blocked until 001 is DONE.
- **003 before 004**: 004 adds tests for the functions it moves and follows the
  test conventions/`@types/bun` setup that 003 establishes.
- 002 and 005 have no dependency beyond 001 (002 needs nothing at all — it is
  docs-only and runs no build).

## Findings considered and rejected

Recorded so future audits do not re-litigate them.

From this audit (2026-06-14):
- **PERF — memoize MapScreen `stageProgress`/`needsMaintenance`; memoize
  `ownedIds` sort; "SORTED_MONSTERS recreated per render".** Not worth doing at
  this scale (≤72 items, O(55) pure math, screens not mounted during rounds). The
  third claim is **factually wrong** — `SORTED_MONSTERS` is module-scoped
  (`CollectionScreen.tsx:13`, before the component), computed once, not per render.
- **esbuild advisory (`GHSA-gv7w-rqvm-qjhr` high, `GHSA-g7r4-m6w7-qqqr` low) via
  vite.** ✅ **RESOLVED 2026-06-14 by upgrading vite 7.3.5 → 8.0.16.** vite 8
  widened its range to `esbuild "^0.27.0 || ^0.28.0"`, pulling a patched esbuild;
  `bun audit` now reports "No vulnerabilities found". (`vite build` re-verified
  green under vite 8 — the major bump introduced no breakage.) Originally low
  real-world impact anyway: the HIGH item was a Deno-installer vector unreachable
  in this bun/Linux project, the LOW one Windows-dev-only, and esbuild is
  build-time only behind a static deploy.
- **CI: add dependency cache + SHA-pin actions (`deploy.yml`).** Saves ~1–2s and
  marginally hardens official actions; low value for a one-child static site.
- **Version-pinning consistency (Biome exact vs others caret).** Cosmetic;
  `bun.lock` already guarantees reproducible installs.
- **store.ts size (657 LOC).** Watch-item, not yet worth a dedicated refactor;
  plan 004 trims it by moving the debug sim out. Revisit only if it grows.
- **`biome.json` itself fails `bun run check` formatting.** Trivial; auto-fixed by
  the mandatory `bun run check --write` and folded into plan 001's closeout.
- **Security: no material findings.** Backend-less static SPA; only input is digit
  presses + dream-monster selection; no `dangerouslySetInnerHTML`/`eval`/`innerHTML`/
  `new Function`; localStorage JSON is guarded by `safeStorage`. Confirmed by read.

Carried over from the prior audit (2026-06-12, commit `89bc634`; those plans were
written then removed in `3398a0d` — re-confirmed still rejected):
- **"Corrupt JSON in `safeStorage.getItem` crashes."** Misread: the discarded
  `JSON.parse(raw)` *is* the validation; a throw lands in `catch` → fallback →
  fresh state. No crash.
- **"README deploy steps don't warn about vite `base`."** They do (`README.md:29`).
- **"CI lacks a typecheck gate."** `bun run build` = `tsc -b && vite build`;
  typecheck is gated. (The missing *test* gate is real and addressed by plan 003.)
- **"Division-by-zero in `shouldUnlockNextStage`."** Guarded — `stageFacts`/pool
  emptiness is handled before any division.

## Execution log (2026-06-14)

All five plans were implemented by `sonnet` executor subagents in isolated git
worktrees and reviewed by the advisor (done-criteria re-run in the worktree,
scope check, full diff read, test-assertion audit). Each lives on its own
`advisor/*` branch; **integration into `main` is the user's call — the advisor
never merges, pushes, or commits to `main`.**

Branch graph (each stacked on its dependency so done-criteria could pass):

    main (8b571d9)
    ├── advisor/002-readme-monster-count         (002)
    └── advisor/001-fix-broken-build             (001)
        ├── advisor/005-gate-reveal-hook         (001 + 005)
        └── advisor/003-test-baseline            (001 + 003)
            └── advisor/004-extract-debug-logic  (001 + 003 + 004)

Worktrees on disk: `/home/zorza/projects/potworki-worktrees/{001,002,003,004,005}`.

Recommended merge (file sets are disjoint and the shared 001 change is identical
across branches → expected conflict-free; `bun install` reconciles `bun.lock`):

    # first commit or stash plans/README.md (it carries uncommitted DONE edits)
    git checkout main
    git merge advisor/004-extract-debug-logic    # brings 001 + 003 + 004
    git merge advisor/005-gate-reveal-hook        # brings 005
    git merge advisor/002-readme-monster-count    # brings 002
    bun install && bun run check && bun test && bun run build

**Integration verified (2026-06-14):** all three leaf branches merged
conflict-free into `advisor/integration` (worktree
`/home/zorza/projects/potworki-worktrees/integration`). Full suite on the merged
result is green: `bun run typecheck` exit 0, `biome check` exit 0, `bun test`
**64 pass / 0 fail**, `bun run build` exit 0 (vite 8). Since the merges were
conflict-free, the simplest integration is a single fast-forward of this branch:

    # commit or stash plans/README.md first (uncommitted DONE edits)
    git checkout main && git merge advisor/integration

Clean up afterwards with `git worktree remove <path>` (removing a worktree keeps
its branch).

Discovery during execution: the repo is on **TypeScript 6.0.3** (from the "bump
deps" commit), which no longer auto-includes `@types/bun`. Test files therefore
begin with `/// <reference types="bun-types" />`. Plan 003 had assumed TS 5.9 +
`skipLibCheck` would suffice; the executor adapted correctly and the pattern is
now established for any new test file.

## Direction findings (not planned — maintainer's call)

Surfaced in the audit report, not turned into plans. Each is grounded in repo
evidence; pick one for a design/spike plan if desired:
- **Save export/import (cross-device).** README explicitly states progress is
  per-device; `SaveState` is versioned + serializable; `safeStorage` exists.
- **Parent/teacher progress view.** `DebugScreen` already computes the per-fact
  mastery table + `needsMaintenance`/`stageProgress`; a PIN-gated read-only screen
  is mostly presentation.
- **Validate the economy after 48 → 72.** `rarityOf` split into frozen + new
  ranges while `RARITY_ODDS` stayed fixed; the existing `simulateRoundOutcome`
  harness could measure whether dup/iskierki pacing still feels right.
- **Audio feedback** for the reward loop (lower grounding; needs mute/autoplay UX).
